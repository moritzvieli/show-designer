import { Fixture } from "./fixture";

// The initial settings of a fixture in a scene
export class SceneFixtureSettings {

    // The fixture holding the settings
    settings: Fixture;

    // The fixture which should have the settings set as a base on the scene
    fixture: Fixture;

}
